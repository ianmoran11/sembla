# GPU throughput spike results

Hardware adapter measurement of the portable WGSL `f32` kernel skeleton.

Generated by `cargo run --release` on this machine.

## Hardware

- Adapter: `Apple M2 Pro`
- Backend: `Metal`
- Device type: `IntegratedGpu`
- Driver: ``
- Driver info: ``
- Timestamp queries: supported
- Shader `f64` capability: unsupported

## Workload and sizes

- Requested scale: 26000000 person rows / 1300000 employer groups
- Actual scale: 26000000 person rows / 1300000 employer groups
- Downscale reason: none (full requested scale fit adapter limits and resident-memory safety budget)
- Warmup ticks: 10
- Measured ticks: 100
- Device-resident data: yes
- Numeric precision: portable WGSL `f32` hazard and race arithmetic; this does not satisfy the production `f64` convention, so the 10k smoke test is exact only against the spike's scalar `f32` implementation
- Steady-state setup: the clear kernel restores the fixed initial enum state before aggregation so every measured tick has the same contested workload
- Aggregate choice: per-employer `atomicAdd` infectious counts (**atomics / Level C fork**, not deterministic reduction)
- Conflict choice: selector `entity_id % 10 == 5` makes exactly 10% of rows eligible (two susceptible contenders per 20-row employer); finite `beta = 100` and `dt = 0.25` make almost all eligible rows candidates
- Actual final-tick argmin candidates: 2582515 (9.933% of all rows); two-pass segmented atomic argmin uses lexicographic `(race_bits, rule_id, entity_id)` tie-break (`rule_id = 0`)
- Final measured-tick fired counter: 19377426

## Steady-state measurements

Median of 100 ticks after 10 warmup ticks. Per-kernel values use pass timestamp pairs where supported. Total timing method: synchronized CPU wall-clock fallback because complete cross-pass GPU timestamps were unavailable.

| Kernel | Median ms/tick |
|---|---:|
| clear | 0.9837 |
| aggregate | 1.1558 |
| hazard/map | 4.2790 |
| argmin race | 1.6754 |
| argmin tie | 1.6699 |
| state write | 2.1631 |
| **Total** | **13.5703** |

- Throughput: **1915.953 million rows/sec**
- Linear extrapolation to 26M rows: **13.5703 ms/tick** (73.690 ticks/sec). This extrapolation is reported even when full scale ran; it is an `f32` kernel-skeleton measurement and is not production-`f64` evidence (nor hardware evidence when the adapter is software).

## Verdict

Against v0.1 success criterion #4, the measured 73.690 ticks/sec at 26M rows makes the core GPU kernel shape plausible on this hardware. However, this portable WGSL path provides only `f32` hazard/race arithmetic, while Sembla's production convention requires `f64`; compliant production-`f64` throughput therefore remains **unanswered on this hardware**, and this rate is optimistic directional evidence rather than a production-backend result. The measured **hazard/map** stage is the bottleneck; v0.2 should first validate a supported `f64` strategy and then profile its bandwidth and contention while retaining deterministic two-pass lexicographic conflict resolution.
