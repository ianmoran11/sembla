## Review

- **Correct:** `libm = "=0.2.16"` is exactly pinned in `crates/sembla-runtime/Cargo.toml:11`, present in `Cargo.lock`, and the direct-dependency allowlist includes only `sha2` and `libm` beyond local `sembla-ir` (`scripts/check.sh:19-30`).
- **Correct:** The sampler uses `libm::log`, `libm::cos`, and `libm::exp` while retaining documented `f64::sqrt` (`crates/sembla-runtime/src/prior.rs:7-12,68-75`). Counters, constant, Box–Muller branch, and expression order remain unchanged.
- **Correct:** Frozen vectors were updated only for the affected LogNormal probe, with old and new bits recorded (`crates/sembla-runtime/tests/prior.rs:43-50`). Distribution and namespace tests were not modified and all three focused sampler tests pass.
- **Correct:** The guard scans runtime/CLI source and self-tests every forbidden method in both method-call forms (`crates/sembla-runtime/tests/transcendental_guard.rs:4-8,36-49,65-85,87-137`). Both guard tests pass.
- **Correct:** `DECISIONS.md` §G6 contains the date, rationale, `sqrt` exemption, dependency policy, and breaking-change statement (`DECISIONS.md:642-670`). The sweep documentation mentions platform-independent θ draws (`docs/examples/sir.md:223-226`).
- **Blocker:** `cargo test --workspace` and `./scripts/check.sh` fail, violating acceptance criteria 1 and 2. `migrated_sir_and_sweep_preserve_legacy_while_policy_reports_every_rule` compares generated output against stale `sweep_draw_1_legacy.csv` at `crates/sembla-cli/tests/observation.rs:106-124`; its sampled `gamma` differs from `crates/sembla-cli/tests/fixtures/sweep_draw_1_legacy.csv:1`. A `--no-fail-fast` run found this was the sole failing target.
- **Blocker:** The audit did not confirm the PRD’s required zero-exemption scope fact. The guard exempts `rng::exp_f64` (`crates/sembla-runtime/tests/transcendental_guard.rs:17-27`), whose platform `ln` occurs at `crates/sembla-runtime/src/rng.rs:125` and feeds simulation scheduling at `crates/sembla-runtime/src/executor.rs:695`. This conflicts with the specification’s “initially: none” exemption list and non-goal assertion that simulation transcendentals do not exist (`docs/prds-portable-sampler/0001-libm-pinned-sampler.md:30-36,54-59`). The PRD must explicitly authorize this exemption or separately resolve the simulation call without silently widening this sampler change.
- **Blocker:** The working diff is not scope-isolated. It includes unrelated licensing changes across the workspace and spikes (`Cargo.toml:13`, `README.md:50-53`, `crates/sembla-cuda/Cargo.toml:5`, spike manifests), two new license files, and unrelated proof-track documents. These are not part of PRD 0001 and should be split from its change set.
- **Note:** `cargo fmt --check` and clippy passed before `scripts/check.sh` reached the failing workspace test. The exact-pin check, dependency tree, and Lean check also pass independently.
- **Note:** There are no staged files. Cross-platform equality still requires CI evidence; this local review only verifies the pinned implementation and vectors.

### PRD acceptance criteria

1. **FAIL:** Workspace tests are not green, although focused sampler tests pass unchanged except for the frozen vector.
2. **FAIL overall:** Exact pin and allowlist checks pass, but `scripts/check.sh` fails during workspace tests.
3. **PASS mechanically, with scope blocker:** The constructed guard self-test passes and unexpected calls fail; however, the current exemption contradicts the PRD’s initial zero-exemption premise.
4. **PASS:** §G6 and user-facing sweep documentation are present.
5. **PASS:** Old `...c9c1` and new `...c9c0` LogNormal bits are documented.

## Recommendation

**REVISE**