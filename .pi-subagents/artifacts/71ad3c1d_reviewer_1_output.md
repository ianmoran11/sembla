## Review

Overall: **FAIL pending two specification/scope blockers**, despite all practical check commands passing.

The requested `/Users/ian/projects/sembla/plan.md` and `/Users/ian/projects/sembla/progress.md` do not exist in the working tree, so this review used the binding `docs/prds-composition/README.md`, `docs/prds-composition/0009-linker-nesting.md`, the actual diff, fixtures, and tests.

### Criterion-by-criterion verification

| PRD area | Result | Evidence |
|---|---|---|
| Canonical artifact bytes | **PASS** | `frontend/scripts/check-parity.sh:187-198` byte-compares regenerated nesting plans and the new wrapped source, then Rust-validates each plan. Independent inspection found sorted object keys, compact UTF-8, no BOM, and no trailing newline in `fixtures/composition-source/wrapped_ping_pong.source.json` and all three new linked plans. `crates/sembla-cli/tests/validate.rs:217-246` also checks Rust canonical reserialization and domain-separated source hashes. |
| Linked shapes/mailbox counts | **PASS** | `fixtures/plans/linked/two_regions.plan.json:1` has 4 leaves and 4 mailboxes; `regional_response.plan.json:1` has 2 leaves and 2 mailboxes; `wrapped_ping_pong.plan.json:1` has 2 leaves and 1 mailbox. These are pinned in `frontend/Sembla/Composition/LinkTests.lean:688-703`. |
| Source-map counts/content | **PASS** | `fixtures/plans/linked/regional_response.plan.json:1` contains boundary `{outer: port:regional_infection_count, leaf: occ:epidemic/population, port: infection_count, path: [expose:regional_infection_count, expose:infection_count]}` and hidden `{instance: inst:epidemic, port: port:restriction_modifier}`. The other two new plans have empty boundary/hidden lists. Lean pins this at `LinkTests.lean:705-720`, and Rust walking validation pins it at `crates/sembla-cli/tests/validate.rs:251-268`. |
| Parity and walking coverage | **PASS** | `frontend/scripts/check-parity.sh:187-198` links and byte-compares `two_regions`, `regional_response`, and `wrapped_ping_pong`; the script passed. `crates/sembla-cli/tests/validate.rs:190-277` now walks exactly 8 linked plans, validates canonicality and provenance, and passed. |
| Exposure adds no delay | **PASS** | `crates/sembla-cli/tests/plan_run.rs:299-337` runs both flat and wrapped ping-pong, asserting `seen_yes=0` at tick 0 and `seen_yes=1` at every tick from tick 1. The test passed. Wrapped exposure adds no mailbox beyond the one root wire. |
| Occurrence/mailbox/word distinctness | **PASS** | `frontend/Sembla/Composition/LinkTests.lean:722-738` pins 4 unique leaf occurrences, 8 unique transition identities, 8 pairwise-distinct rule words, and distinct north/south mailbox identities. Independent JSON inspection confirmed the counts and identities. `lake build` passed all guards. |
| Display rename semantic law | **FAIL (blocker)** | The semantic core comparison and stable IDs behave correctly, but the required byte law is weakened. `LinkTests.lean:740-745` blanks both `sourceHash.digest` **and** the source map before comparison; `LinkTests.lean:768-774` explicitly asserts the source hashes differ. PRD §4.3 required comparison with only `source_map` blanked (plan bytes identical except source map). Also, display names are not actually carried by the source map: `frontend/Sembla/Composition/Link.lean:1415-1419` sets `displayPath := leaf.modelBox.name`, which is the stable occurrence path (`north/population`), not the renamed display-name path. This conflicts with the binding README rule that display names live only in the source map. |
| Declaration permutation byte law | **FAIL (blocker)** | Definitions, instances, wires, and exposures are all permuted, and core/source-map equality is tested. However, `LinkTests.lean:803-814` blanks `sourceHash.digest` before rendered-plan comparison and explicitly requires the original and variant source digests to differ. PRD §4.4 requires **full linked-plan byte equality including source map**. The guard therefore does not establish the stated byte law. Exact source-byte provenance appears to make the written full-plan law incompatible with author-order changes; that contract conflict must be resolved explicitly rather than masked in the test. |
| Stable-ID rename divergence | **PASS** | `LinkTests.lean:824-839` renames `inst:north` to `inst:west`, updates the summary path, and guards that the semantic digest changes. `lake build` passed. |
| Every requested visibility negative | **PASS** | `LinkTests.lean:302-317,346-431` pins: unexposed composite descendant → `inaccessibleDescendantPort` with composite named; missing exposed child port → `missingPort`; duplicate outer port → `duplicateStableId`; hidden+wired and hidden+exposed → `hiddenPortConflict`; wire to hidden boundary → documented `hiddenPortConflict`; exposed input used as source → `directionMismatch`. It also pins declared exposure direction/schema mismatches. `hiddenPortConflict` is the sole new error constructor at `frontend/Sembla/Composition/Errors.lean:7-15,25-42`. |
| `two_regions` run divergence and hashes | **PASS** | `fixtures/plans/goldens/two_regions.seed55.ticks8.csv:4-11` has north I trace `629,651,657,659,658,656,649,643` and south `634,656,654,646,652,651,645,633`; they differ from tick 0 onward. `crates/sembla-cli/tests/plan_run.rs:527-557` pins both traces. The reproduced hashes are results `3421eeb8ff3ea6f3282a665982c8aa9f2c92e7b0c0a169fa5dc8a82ed5158562`, final state `f1473170e2d6bbdc1b6a86d80a993e6e13f039ee75684cda08f9f97c92875a85`, observation `8b525963dcb78088f873037639dcda91be9eecc81a32c1622e2d63afdb263fd1`. |
| `regional_response` run golden/hashes | **PASS** | `crates/sembla-cli/tests/plan_run.rs:466-525` reproduces its CSV and all reported hashes. Values: results `2df40c21fb7cb3457b847a779b7904326e854b4982ab7fa2fdfe26dd7f22992d`, final state `b1bae0c299750c609634c07810a2eb0f9b220bf2c41230f6995b5ef402cca8d4`, observation `2c0664afac18f766ff9366681be3f5bc1e309f78889484d6152722b78c7a88bf`. |
| Legacy artifact preservation | **PASS** | No tracked `examples/**` file or pre-existing golden is changed. Full `./scripts/check.sh` passed the legacy canonical/golden suites, and `crates/sembla-cli/tests/plan_run.rs:564+` passed the pre-PRD legacy manifest byte baseline. |
| Required checks | **PASS** | `./scripts/check.sh`, `cd frontend && lake build`, `bash frontend/scripts/check-parity.sh`, and `git diff --check` all passed. Targeted CLI `plan_run` and `validate` tests also passed (7/7 and 11/11). |
| Allowed scope / frozen runtime | **FAIL (blocker)** | `crates/sembla-cli/src/main.rs:1805-1929` changes production population initialization from exactly one compatible population/controller pair to multiple occurrence-scoped pairs. PRD 0009 permits only `crates/sembla-cli/tests/**` and `crates/sembla-ir/tests/**` on the Rust side and says “Runtime changes of any kind” are a non-goal. In addition, tracked `.piprd/log.jsonl` and `.piprd/run-state.json` are modified even though the binding README says “No editing of `.piprd/`.” The runtime-adjacent CLI change may be needed to generate the requested nested run goldens, revealing another PRD scope tension, but it is outside the accepted file list as written. |
| Staging state | **PASS** | `git diff --cached --quiet` returned 0; there are no staged files. |

### Findings

- **Blocker:** `frontend/Sembla/Composition/LinkTests.lean:740-822` — display/permutation tests mask differing source hashes, so they do not prove the PRD’s requested rendered-plan byte laws. Display paths also discard display names at `frontend/Sembla/Composition/Link.lean:1415-1419`.
- **Blocker:** `crates/sembla-cli/src/main.rs:1805-1929`, `.piprd/log.jsonl`, `.piprd/run-state.json` — changes exceed PRD 0009’s allowed files and frozen runtime/managed-state scope.
- **Correct:** Boundary resolution, transitive exposure, mailbox identity/counts, source-map boundary/hidden content, no-delay behavior, occurrence/word uniqueness, stable-ID divergence, visibility errors, canonical parity, walking validation, and run goldens are all concretely exercised and passing.
- **Note:** The absent requested `plan.md`/`progress.md` prevented comparison against any local implementation plan or progress claims.