## Review

- **Blocker: none.** No code blocker remains in the requested aggregate/staging/promotion areas based on the diff, static CUDA/host ABI audit, and host-runnable tests.
- **High: none.**
- **Medium: none.**
- **Low — exact diagnostic regressions are only weakly frozen by tests.** The implementation now reports semantic aggregate/field identities and deterministically preserves the first status: `sembla_check_aggregate_errors` accepts `aggregate_index`, clears the per-launch buffer, and writes it only when status is still clear (`crates/sembla-cuda/src/codegen.rs:989`); the host invokes that check immediately after each aggregate in schedule, effect, and output order (`crates/sembla-cuda/src/backend.rs:413-455`, `559-602`, `649-691`). Output errors are reduced in ascending flattened field order and report `field`, not a partial/merge slot (`crates/sembla-cuda/src/codegen.rs:1408`). However, the new host test only checks that generated source contains `status[1] = aggregate_index` and `status[1] = field` (`crates/sembla-cuda/src/codegen.rs:1812-1819`); the GPU aggregate-error test merely checks that the message contains `aggregate` (`crates/sembla-cuda/tests/gpu_semantics.rs:476-491`), and there is no multi-failure exact aggregate/output identity-and-precedence assertion. This is a coverage gap, not an observed implementation defect.

### Verified requested fixes

- Aggregate use is classified by schedule/effect/wired-output reachability (`crates/sembla-cuda/src/codegen.rs:213-263`), structurally shared uses are merged and nested dependencies are collected first (`crates/sembla-cuda/src/codegen.rs:327-373`), and stable phase lists are emitted (`crates/sembla-cuda/src/codegen.rs:799-826`). Effect-only aggregates are activated from winning rows (`crates/sembla-cuda/src/codegen.rs:912-928`); schedule aggregates are not redundantly staged as effect-only (`crates/sembla-cuda/src/codegen.rs:805-811`).
- Schedule/effect reductions read tick-start `state`, while wired output aggregates and output fields read prospective `next_state` (`crates/sembla-cuda/src/backend.rs:413-431`, `559-578`, `646-724`), matching CPU pre-commit Moore-output construction (`crates/sembla-runtime/src/executor.rs:342-355`). Unwired outputs are excluded (`crates/sembla-cuda/src/codegen.rs:238-263`) and frozen by the host test at `crates/sembla-cuda/src/codegen.rs:1747-1754`; nested and shared cases are frozen at `crates/sembla-cuda/src/codegen.rs:1736-1745`, `1811-1819`.
- Winning-effect liveness matches the CPU behavior: aggregate activation is winner-based, while a transition with any winner evaluates its effect column for every source row; the real-GPU losing-row overflow fixture is at `crates/sembla-cuda/tests/gpu_semantics.rs:454-473`.
- Input-row ordered numeric comparisons promote both operands, including Int/Int, to `double` (`crates/sembla-cuda/src/codegen.rs:634-649`), with source and differential fixtures at `crates/sembla-cuda/src/codegen.rs:1802-1809` and `crates/sembla-cuda/tests/gpu_semantics.rs:385-410`.
- The canonical two-half schedule is now documented and implemented consistently: evaluator module contract (`crates/sembla-runtime/src/eval.rs:3-8`), input reductions (`crates/sembla-runtime/src/eval.rs:961-1023`), grouped reductions (`crates/sembla-runtime/src/eval.rs:1544-1655`), and outputs (`crates/sembla-runtime/src/executor.rs:862-904`).
- Kernel/host ABI argument order and widths match for the changed aggregate/output path: generated aggregate signatures are at `crates/sembla-cuda/src/codegen.rs:932`, `973`, `989`, with corresponding launch arguments at `crates/sembla-cuda/src/backend.rs:417-455` (and the identical effect/output-phase launches at `559-602`, `653-691`); output signatures are at `crates/sembla-cuda/src/codegen.rs:1291`, `1305`, `1370`, `1408`, with launches at `crates/sembla-cuda/src/backend.rs:693-740`. No missing, reordered, or width-mismatched argument was found.

### Validation and real-GPU boundary

- Passed: `git diff --check`; `cargo fmt --all -- --check`; `cargo build --workspace --locked`; `cargo test --workspace --locked`; `cargo check -p sembla-cuda --features cuda --all-targets --locked`; `cargo test -p sembla-cuda --features cuda --tests --locked`; `cargo test -p sembla-cuda --features cuda --lib --locked`; `cargo test -p sembla-runtime --locked`.
- The CUDA-feature run passed all host-runnable tests (14 CUDA lib tests plus the absence and semantic-fixture tests); 4 oracle, 1 Philox, and 8 GPU-semantic tests remained ignored.
- **Real-GPU unanswered:** this host has neither `nvidia-smi` nor `nvcc`, so NVRTC compilation, actual kernel parameter ABI at launch, device Philox equality, SIR/`sir_policy`/canonical per-tick hash equality, Level-A repeatability, and the requested staging/promotion/nested/effect diagnostic fixtures were not executed. This is correctly recorded as unanswered in `crates/sembla-cuda/GPU-STATUS.md:3-16`, as permitted by PRD acceptance criterion 6 (`docs/prds-npe-path/0008-cuda-backend.md:68-78`).
