{
  "kind": "advice",
  "createdAt": "2026-07-19T01:25:17.185Z",
  "runId": "2026-07-18T13-51-30-693Z",
  "prdPath": "docs/prds-npe-path/0008-cuda-backend.md",
  "absolutePath": "/Users/ian/projects/sembla/docs/prds-npe-path/0008-cuda-backend.md",
  "attempt": 2,
  "reviewText": "## Assessment: REVISE\n\n### Blocking issue\n\n- **Parallel conflict resolution has a result-bearing data race for incompatible claim orderings.**  \n  `sembla_resolve_conflicts` runs one thread per candidate (`crates/sembla-cuda/src/backend.rs:513-536`, generated at `crates/sembla-cuda/src/codegen.rs:949`). When enabled candidates claim the same resource with incompatible ordering modes, every involved thread writes directly to shared `status[0..2]` (`codegen.rs:1007-1012`) without atomics or unique error slots. A valid model using, for example, race-time ordering in one transition and key ordering in another can reach this path. The resulting diagnostic fields—and formally the writes themselves—are nondeterministic, violating the Level A and exact CPU-oracle contracts. Move compatibility detection into the serial `sembla_validate_claims` phase or write per-candidate errors followed by deterministic reduction before parallel argmin.\n\n### Acceptance criteria\n\n| Criterion | Result |\n|---|---|\n| 1. Toolkit-free workspace build/test | **Pass** — locked workspace build and tests pass; deterministic codegen and updated SIR golden run everywhere. |\n| 2. Device Philox equality | **Unanswered as permitted** — real ignored device test is compiled and discoverable. |\n| 3. Required oracle-equality cases | **Unanswered as permitted** — SIR 100k/200, `sir_policy`, and canonical tests exist. |\n| 4. Level A repeatability | **Unanswered as permitted** — repeat-run GPU test exists. |\n| 5. No-device diagnostic | **Pass** — production `CUDA_ERROR_NO_DEVICE` mapping and frozen diagnostic are non-ignored tests. |\n| 6. Honest reporting | **Pass** — `GPU-STATUS.md` records 2–4 as unanswered and links the provenance-producing remote runner. |\n\nThe prior blockers concerning two-part reductions, checked multiplication, eager claim/effect/filter evaluation, destination-ordered effects, and prospective-state wire aggregates are otherwise addressed.\n\nVerification passed:\n\n- `cargo build --workspace --locked`\n- `cargo test --workspace --locked`\n- CUDA-feature all-target check\n- CUDA-feature clippy with `-D warnings`\n- GPU test inventory\n- Remote-runner shell validation\n- `git diff --check`\n\n```json\n{\"assessment\":\"REVISE\",\"blockingIssues\":[\"Parallel conflict-resolution threads write the shared status tuple concurrently when incompatible claim orderings meet on one resource; move this error detection to the serial validation phase or deterministically reduce per-candidate errors before argmin.\"],\"summary\":\"All six acceptance-criterion surfaces are implemented or honestly marked unanswered, and prior revision blockers are resolved, but one reachable shared-status race remains in incompatible claim-order handling, so the generic Level A backend is not yet deterministic for every validated model.\"}",
  "adviceText": "## Recommended course\n\nMake one narrowly scoped CUDA fix: move incompatible-ordering detection out of the candidate-parallel resolver and into the existing single-thread claim-validation kernel. Do not reopen reductions, effects, runtime semantics, or reporting.\n\n## Root cause\n\n`sembla_resolve_conflicts` is launched with one thread per candidate:\n\n- `crates/sembla-cuda/src/backend.rs:513-536`\n- Generated in `crates/sembla-cuda/src/codegen.rs:949+`\n\nEvery enabled candidate independently scans competing claims. When an incompatible pair is found, multiple threads can execute:\n\n```cpp\nstatus[0] = 4ULL;\nstatus[1] = self_candidate;\nstatus[2] = other_candidate;\n```\n\nThe initial `status[0] != 0` check is not synchronization. Threads can interleave these writes, yielding a mixed or nondeterministic status tuple. CUDA stream ordering only orders kernels, not threads within this kernel.\n\nUsing atomics is not an acceptable repair:\n\n- The PRD forbids atomics on result-bearing paths.\n- An atomic status code would not make the separate candidate fields deterministic.\n- Which thread wins would still be scheduler-dependent.\n\n## Smallest safe implementation\n\n### 1. Extend the serial validation phase\n\nUpdate generated `sembla_validate_claims` in `crates/sembla-cuda/src/codegen.rs`.\n\nKeep two distinct phases inside that one-thread kernel:\n\n1. **Existing eager validation pass**\n   - Evaluate every claim resource and key in current canonical order.\n   - Do not skip disabled candidates.\n   - Return status 10 for the first claim-expression error.\n2. **New compatibility pass**\n   - Consider only enabled candidates.\n   - Compare claims only within the same box and target resource table.\n   - Emit checks only for statically incompatible claim-descriptor pairs.\n   - At runtime, report status 4 only when those candidates reference the same resource row.\n\nKeeping compatibility after the complete eager pass preserves CPU error precedence: a later claim-expression overflow must not be hidden by an earlier incompatible collision.\n\nPass `enabled` into `sembla_validate_claims` from `backend.rs`.\n\n### 2. Use canonical pair enumeration\n\nGenerate each unordered static claim pair once, ordered by something stable such as:\n\n```text\n(box index, rule ID, claim index)\n```\n\nWithin each pair, scan rows ascending. The first matching enabled collision then produces a deterministic candidate tuple.\n\nEnsure this covers two different claims belonging to the same candidate. Do not deduplicate solely with `other_candidate > self_candidate`, because distinct claims on one candidate can resolve to the same resource and still be incompatible.\n\nEmitting runtime loops only for statically incompatible pairs avoids adding an `O(N²)` serial pass to normal models whose claim ordering domains are all compatible.\n\n### 3. Make the parallel resolver read-only with respect to status\n\nRemove the status-4 branch from `sembla_resolve_conflicts`.\n\nPrefer changing its generated parameter to:\n\n```cpp\nconst unsigned long long* status\n```\n\nand pass `&self.status`, not `&mut self.status`, in `backend.rs`. After the serial preflight, resolver threads should only:\n\n- Read immutable state/status.\n- Write their unique `wins[self_candidate]` cell.\n\nNo shared result-bearing write should remain in that kernel.\n\n## Tests to add\n\n### Always-running codegen regression\n\nIn `crates/sembla-cuda/src/codegen.rs`, add a valid model with two transitions that:\n\n- Are both capable of claiming the same resource.\n- Use incompatible ordering classes, such as `RaceTime` and `Key(Int)`.\n\nAssert that generated source:\n\n- Contains status-4 handling in `sembla_validate_claims`.\n- Passes `enabled` to that kernel.\n- Contains no `status[...] = ...` write inside the `sembla_resolve_conflicts` source region.\n- Still contains no result-bearing atomics.\n\n### Ignored GPU semantic regression\n\nIn `crates/sembla-cuda/tests/gpu_semantics.rs`:\n\n1. Confirm the CPU oracle reports `IncompatibleClaimOrdering`.\n2. Confirm CUDA reports incompatible ordering.\n3. Repeat the CUDA failure and assert the complete diagnostic is identical.\n\nAlso test one false-positive case:\n\n- One incompatible contender is disabled, or\n- The contenders reference different resource rows.\n\nCPU and CUDA must both proceed without an incompatibility error. This prevents an unsafe model-load/static rejection based solely on ordering types.\n\n## Files expected to change\n\n- `crates/sembla-cuda/src/codegen.rs`\n- `crates/sembla-cuda/src/backend.rs`\n- `crates/sembla-cuda/tests/gpu_semantics.rs`\n- `crates/sembla-cuda/tests/fixtures/sir.generated.cu` — regenerate; do not hand-edit\n\nNo changes should be necessary in:\n\n- `crates/sembla-runtime/src/executor.rs`\n- `crates/sembla-runtime/src/eval.rs`\n- `crates/sembla-cuda/src/error.rs`\n- The PRD itself\n\nOnly update `GPU-STATUS.md` if genuine GPU evidence is obtained.\n\n## Important risks\n\n- **Error precedence regression:** Do not interleave compatibility detection with eager claim evaluation.\n- **Over-rejection:** Incompatible ordering types are legal unless enabled candidates collide on the same resource.\n- **Missed same-candidate collisions:** Canonicalize claim descriptors, not only candidate IDs.\n- **Cross-box confusion:** Resource table names are box-local; retain the existing box identity check.\n- **CUDA ABI mismatch:** The generated kernel signature and `backend.rs` launch arguments must change together.\n- **Golden drift:** Regenerate the SIR fixture after changing shared kernel signatures.\n- **Generated CUDA is not compiled by `cargo check`:** A feature build validates Rust but not every generated CUDA translation unit.\n\n## Verification\n\n```bash\ncargo run -p sembla-cuda --example generate_sir_golden\n\ncargo fmt --all -- --check\ncargo build --workspace --locked\ncargo test --workspace --locked\n\ncargo check -p sembla-cuda --features cuda --all-targets --locked\ncargo clippy -p sembla-cuda --features cuda --all-targets --locked -- -D warnings\ncargo test -p sembla-cuda --features cuda --no-run --locked\ncargo test -p sembla-cuda --features cuda -- --list\n\nbash scripts/check.sh\ngit diff --check\n```\n\nIf CUDA hardware is available, finish with:\n\n```bash\ncrates/sembla-cuda/scripts/run-gpu-tests.sh\n```\n\nOtherwise keep acceptance criteria 2–4 marked **unanswered**. This targeted change should resolve the sole review blocker without disturbing the already-approved acceptance surfaces."
}