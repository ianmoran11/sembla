{
  "kind": "advice",
  "createdAt": "2026-07-19T00:27:27.339Z",
  "runId": "2026-07-18T13-51-30-693Z",
  "prdPath": "docs/prds-npe-path/0008-cuda-backend.md",
  "absolutePath": "/Users/ian/projects/sembla/docs/prds-npe-path/0008-cuda-backend.md",
  "attempt": 1,
  "reviewText": "## Assessment: REVISE\n\n### Blocking issues\n\n1. **Required fixed-shape two-pass reductions are not implemented.**  \n   `crates/sembla-cuda/src/codegen.rs:834` emits aggregate reduction as a single-thread kernel, while `crates/sembla-cuda/src/backend.rs:341-358` launches it with one block and one thread. Conflict resolution, effects, and outputs are similarly single-thread launches. Calling this a “one-leaf” two-pass tree in `crates/sembla-cuda/README.md:37-42` does not satisfy the specified fixed-shape two-pass reduction architecture.\n\n2. **CUDA expression/executor edge semantics diverge from the CPU oracle.**\n   - `crates/sembla-cuda/src/codegen.rs:1215-1216` performs signed `a * b` before detecting overflow. Signed C++ overflow is undefined behavior, unlike CPU `checked_mul` at `crates/sembla-runtime/src/eval.rs:1179,1268`.\n   - Claim expressions are skipped for disabled candidates at `codegen.rs:933`, whereas CPU claim columns are evaluated before eligibility filtering at `executor.rs:665-680`. Generated claim-key errors also are not checked after evaluation.\n   - Effects are evaluated only for winners at `codegen.rs:1040`; CPU evaluates each effect over the full table once a transition has any winner (`executor.rs:730-757`).\n   - Group/output sum values are conditionally evaluated under their filters (`codegen.rs:862-876,1109-1113`), while CPU grouped/output evaluation builds the full value column first (`eval.rs:1529-1563`, `executor.rs:845-864`).\n\n   These are reachable success-versus-error differences for valid models, violating the requirement to mirror evaluator operations and edge cases exactly.\n\n3. **Nested aggregates in wire outputs can use stale state.**  \n   Validated output expressions may contain `Expr::Agg`. CUDA builds aggregate buffers before applying effects and then reuses them when building outputs from `next_state` (`backend.rs:346-470`). CPU output evaluation creates a new aggregate cache over the prospective committed state. Thus a wire output containing a nested aggregate can observe tick-start aggregates instead of prospective state.\n\n4. **Actual no-device construction behavior is not proven and may return the wrong diagnostic.**  \n   `crates/sembla-cuda/tests/absence.rs:3-15` tests only synthetic `CudaAvailability` facts, not `CudaBackend::new`. The production constructor maps any `CudaContext::device_count()` failure directly to `CudaError::Driver` at `backend.rs:101`; CUDA initialization may report `CUDA_ERROR_NO_DEVICE` through that error path before a zero count reaches `check_availability`. Criterion 5 therefore is not satisfied by the production path.\n\n### Acceptance criteria\n\n| Criterion | Result |\n|---|---|\n| 1. Toolkit-free workspace build/test and non-GPU tests | **Pass** — locked workspace build/test passed; codegen and SIR golden tests ran. |\n| 2. Device Philox equality | **Unanswered as permitted** — realistic ignored device test exists. |\n| 3. Three oracle-equality cases | **Unanswered as permitted** — required ignored tests compile and are discoverable. |\n| 4. Level A repeatability | **Unanswered as permitted** — repeat-run GPU test exists. |\n| 5. No-device construction diagnostic | **Fail** — synthetic classifier test does not cover the production probe/error path. |\n| 6. Honest GPU reporting | **Pass** — `GPU-STATUS.md` records 2–4 as unanswered and links the remote runbook. |\n\nVerification performed:\n\n- `cargo build --workspace --locked` — passed\n- `cargo test --workspace --locked` — passed\n- CUDA-feature all-target check — passed without requiring a local toolkit\n- CUDA test inventory — all five ignored GPU tests present\n- `git diff --check` — passed\n\n```json\n{\"assessment\":\"REVISE\",\"blockingIssues\":[\"Generated reductions and shared result kernels are single-thread serial kernels, not the required fixed-shape two-pass reductions.\",\"CUDA integer overflow and claim/effect/filtered-aggregate evaluation semantics observably diverge from the CPU oracle.\",\"Wire output expressions containing nested aggregates reuse tick-start aggregate buffers instead of recomputing against prospective state.\",\"The local no-device test bypasses CudaBackend::new, whose device_count error path can return a generic Driver diagnostic instead of NoDevice.\"],\"summary\":\"Toolkit-free checks, ignored GPU harnesses, and honest unanswered reporting are present, but production ordering architecture, exact oracle semantics, prospective-state wire aggregates, and actual no-device construction behavior require additional implementation.\"}",
  "adviceText": "## Recommendation\n\nDo not patch the four findings independently inside the current monolithic kernels. The root problem is that evaluation, filtering, reduction, resolution, and mutation are fused together. The next revision should introduce explicit device staging phases and a shared deterministic schedule mirrored by the CPU oracle.\n\n## Root causes\n\n| Blocker | Root cause |\n|---|---|\n| No real two-pass reductions | CUDA codegen uses one thread to initialize and fold each reduction. The README reinterprets this as a “one-leaf tree,” but no partial/final reduction stages exist. |\n| Edge-semantics divergence | Expressions are inlined behind eligibility/filter/winner conditions instead of being evaluated with the CPU executor’s column-oriented timing. Integer multiplication also checks overflow only after invoking undefined signed overflow. |\n| Stale wire aggregates | Only one aggregate buffer is built from tick-start state. Output kernels receive `next_state` but still read aggregates derived from old state. |\n| Wrong no-device path | Availability classification starts after `CudaContext::device_count()`. A `CUDA_ERROR_NO_DEVICE` from initialization is converted immediately into generic `CudaError::Driver`. The test bypasses this path. |\n\n## Best implementation path\n\n### 1. Freeze the deterministic reduction contract first\n\nThere is a genuine contract conflict:\n\n- `sembla-runtime` currently uses ascending-row sequential folds.\n- The PRD and `DESIGN.md` require fixed-shape two-pass trees.\n- A true two-pass tree necessarily reassociates floating-point sums.\n\nThe smallest literal implementation is to adopt the spike’s **two-part schedule** in both backends:\n\n1. Divide the canonical target-row range into two fixed contiguous halves.\n2. Accumulate each half in ascending row order.\n3. Merge partial 0, then partial 1.\n\nFor grouped reductions, each partial owns a complete group accumulator array, so two workers can scan their respective row halves without atomics. The final pass launches one worker per group and computes `partial0 + partial1`.\n\nThis requires changing the CPU oracle in `crates/sembla-runtime/src/eval.rs` to the same schedule. Do not retain sequential CPU folds while introducing a balanced CUDA reduction—the hashes cannot then be guaranteed equal.\n\nRelevant files:\n\n- `spikes/precision/src/cuda/f64_native.cu`\n- `spikes/precision/src/f64_mirror.rs`\n- `crates/sembla-runtime/src/eval.rs`\n- `crates/sembla-runtime/tests/eval.rs`\n- `crates/sembla-cuda/src/codegen.rs`\n- `crates/sembla-cuda/src/backend.rs`\n\nAdd a CPU test using an order-sensitive vector such as:\n\n```text\n[1e16, 1.0, -1e16, 1.0]\n```\n\nIt must freeze the chosen two-pass result. Expect intentional changes to model hashes that consume real aggregates. If changing the CPU oracle is unacceptable, the PRD and `DESIGN.md` must be amended; another “degenerate tree” explanation will likely be rejected.\n\n### 2. Split evaluation from filtering and mutation\n\nIntroduce device-resident staging buffers rather than rendering expressions directly inside `if` statements.\n\nRecommended tick phases:\n\n1. Build old-state aggregate partials.\n2. Finalize old-state aggregates.\n3. Evaluate transition guard/hazard columns.\n4. Evaluate all claim resource/key columns.\n5. Check canonical expression errors.\n6. Generate candidate flags/race times.\n7. Resolve claims.\n8. If a transition has any winner, evaluate every effect over its full table.\n9. Stage winner write records.\n10. Sort/check/apply writes.\n11. Rebuild output-dependent aggregates against `next_state`.\n12. Build next wire inputs.\n13. Check status, then swap state/input buffers.\n\nThe staging rules must mirror the CPU precisely:\n\n- Claims: evaluate full columns before eligibility filtering.\n- Effects: skip entirely if a transition has no winners; otherwise evaluate every row.\n- Group `Agg::Sum`: evaluate the complete value column, then apply the filter.\n- Output `Sum`: evaluate the complete value column, then apply the filter.\n- Input aggregate `Sum`: retain its existing CPU behavior—value evaluation occurs only for selected input rows.\n\nRelevant files:\n\n- `crates/sembla-cuda/src/codegen.rs`\n- `crates/sembla-cuda/src/backend.rs`\n- `crates/sembla-runtime/src/eval.rs`\n- `crates/sembla-runtime/src/executor.rs`\n\n### 3. Replace result-bearing serial scans with deterministic records\n\nUse fixed, device-resident record buffers:\n\n- **Claim records:** resource identity, ordering kind/value, rule, entity, claim index.\n- **Write records:** destination cell, rule, entity/effect order, typed value payload.\n- **Aggregate records/partials:** group and partial ownership.\n\nA bottom-up stable merge sort is a reasonable minimal GPU sorter:\n\n- fixed merge widths: 1, 2, 4, …\n- one worker owns each output merge range;\n- ping-pong buffers;\n- deterministic comparator;\n- no atomics;\n- global synchronization through kernel-launch boundaries.\n\nUse it to:\n\n- group claims by resource and establish lexicographic argmin;\n- sort writes by destination;\n- detect duplicate destinations;\n- scatter unique writes.\n\nThis is simpler and less memory-intensive than a padded bitonic network while remaining deterministic.\n\n### 4. Fix integer multiplication independently\n\nReplace:\n\n```cpp\nlong long result = a * b;\nif (result / b != a) ...\n```\n\nwith a pre-multiplication bounds check that never performs overflowing signed arithmetic. Avoid relying on NVRTC support for compiler builtins unless it is verified and pinned.\n\nAdd vectors covering:\n\n- `i64::MAX * 2`\n- `i64::MIN * -1`\n- positive × negative overflow\n- non-overflow boundary products\n- zero\n\nTest the helper through actual device expression evaluation, not source-string matching alone.\n\n### 5. Recompute prospective-state aggregates\n\nAfter effects have produced `next_state`, rerun the aggregate partial/final stages for aggregates reachable from output filters/values. Inner aggregate dependencies are already collected before outer expressions; preserve that stable dependency order.\n\nEither:\n\n- maintain separate old/next aggregate buffers, or\n- reuse the old aggregate buffer only after every old-state claim/effect evaluation is finished.\n\nThen run output construction with:\n\n- `next_state`\n- prospective aggregates\n- current tick’s input tables\n\nThis fixes nested `Expr::Agg` in wire outputs without introducing a second execution path.\n\n### 6. Make no-device probing part of the constructor path\n\nAdd an internal probe abstraction used directly by `CudaBackend::new`:\n\n```text\ndriver library → CUDA initialization/device count → NVRTC presence\n```\n\nMap `cudarc::driver::DriverError(CUDA_ERROR_NO_DEVICE)` explicitly to `CudaError::NoDevice`; other driver failures remain `CudaError::Driver`.\n\nA fake probe should drive the same internal constructor path and return early before context creation. Add non-ignored cases for:\n\n- driver missing;\n- `CUDA_ERROR_NO_DEVICE`;\n- zero device count;\n- NVRTC missing;\n- unrelated driver error.\n\nRelevant files:\n\n- `crates/sembla-cuda/src/backend.rs`\n- `crates/sembla-cuda/src/availability.rs`, or a new `probe.rs`\n- `crates/sembla-cuda/src/error.rs`\n- `crates/sembla-cuda/tests/absence.rs`\n\n## Recommended implementation order\n\n1. Add failing CPU/CUDA semantic fixtures for all reported divergences.\n2. Fix no-device mapping and checked multiplication.\n3. Define and test the shared two-part reduction schedule on CPU.\n4. Add CUDA partial/final buffers and kernels.\n5. Introduce expression staging and canonical error checks.\n6. Recompute aggregates against prospective state before wire outputs.\n7. Add deterministic claim/write record sorting and scatter.\n8. Regenerate the SIR CUDA golden fixture.\n9. Update README wording to describe the actual schedule.\n10. Run remote GPU tests only after the tree is committed and clean.\n\n## Tests to add\n\nNon-GPU:\n\n- two-pass reduction order golden;\n- generated source contains distinct partial/final kernels;\n- no result-bearing atomic instructions;\n- deterministic merge-pass/symbol ordering;\n- no-device CUDA error classification;\n- checked-multiplication source/vector tests;\n- prospective aggregate dependency ordering.\n\nIgnored GPU tests:\n\n- integer overflow parity;\n- disabled-row claim-key overflow parity;\n- losing-row effect overflow parity;\n- filtered-out group/output value overflow parity;\n- nested aggregate wire output after an effect;\n- conflicting claims and duplicate-write behavior;\n- order-sensitive two-pass reduction vector;\n- racing-clock `to_bits()` vectors, because CUDA `log` versus CPU `ln` remains a material risk.\n\n## Verification gates\n\nRun after each major stage:\n\n```sh\ncargo fmt --all -- --check\ncargo clippy --workspace --all-targets -- -D warnings\ncargo build --workspace --locked\ncargo test --workspace --locked\ncargo check -p sembla-cuda --features cuda --all-targets --locked\ncargo test -p sembla-cuda --features cuda -- --list\nbash scripts/check.sh\ngit diff --check\n```\n\nAfter approval and commit, use:\n\n```sh\ncrates/sembla-cuda/scripts/run-gpu-tests.sh\n```\n\nUntil that genuine GPU run succeeds, keep criteria 2–4 marked **unanswered**.\n\nThe main risk is the intentional CPU reduction-order change: it may alter existing hashes and decision boundaries. Treat every changed golden/hash as requiring explanation, not mechanical regeneration."
}